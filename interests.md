# Discovery Interests

What `gtm_agent/discover.py` searches. Edit freely — it's parsed by section heading,
so keep the `## Accounts` and `## Topics` headings, and keep entries as `-` bullets.
Blank sections are fine. Lines that aren't bullets are ignored, so add notes anywhere.

## Accounts

Usernames to pull recent posts from. The `@` is optional.

- @Shahules786

## Topics

Search terms for the recent-search endpoint (last 7 days only). One idea per bullet.
Availability on pay-per-use is unconfirmed — if the endpoint isn't callable on your
plan, discovery falls back to accounts-only and says so.

Disabled (pulled only noise — make it a `-` bullet again to re-enable):
agent evaluation benchmark
LLM tool calling
